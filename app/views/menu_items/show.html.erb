<table>
  <tr>
    <td>Name:</td>
    <td><%= @menu_item.name %></td>
  </tr>
  <tr>
    <td>Description:</td>
    <td><%= @menu_item.description %></td>
  </tr>
  <tr>
    <td>Category:</td>
    <td><%= @menu_item.category %></td>
  </tr>
  <tr>
    <td>Price:</td>
    <td><%= @menu_item.price %></td>
  </tr>
</table>

<%= link_to "Back", root_path %><br>
<%= link_to "Edit", edit_menu_item_path(@menu_item) %><br>
<%= link_to "Delete Menu Item", menu_item_path(@menu_item), method: :delete, data: { confirm: "Are you sure?" } %>
